export enum ApiRoutes {
  AUTH = '/auth',
  USER = '/user',
  BOARD = '/boards',
}

export enum AppRoutes {
  DASHBOARD = '/dashboard',
  LOGIN = '/login',
  REGISTER = '/register',
  BOARD = '/dashboard/board',
}
