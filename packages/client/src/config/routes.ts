export enum ApiRoutes {
  AUTH = '/auth',
  USER = '/user',
  BOARD = '/board',
}

export enum AppRoutes {
  DASHBOARD = '/dashboard',
  LOGIN = '/login',
  REGISTER = '/register',
  BOARD = '/dashboard/board',
}
